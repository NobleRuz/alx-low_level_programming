#include <stdio.h>
/**
 * main - Entry point
 * My Hello, printf
 * Return: return 1 after executing a program
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
